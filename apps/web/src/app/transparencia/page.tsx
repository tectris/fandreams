'use client'

import { LegalPage } from '@/components/legal-page'

export default function TransparencyReportPage() {
  return (
    <LegalPage
      pageKey="transparency_report"
      fallbackTitle="Relatorio de Transparencia"
      fallbackMessage="O relatorio de transparencia sera publicado em breve."
    />
  )
}
