'use client'

import { LegalPage } from '@/components/legal-page'

export default function CreatorContractPage() {
  return (
    <LegalPage
      pageKey="creator_contract"
      fallbackTitle="Contrato do Criador"
      fallbackMessage="O contrato do criador sera publicado em breve."
    />
  )
}
