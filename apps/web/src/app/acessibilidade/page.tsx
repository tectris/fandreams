'use client'

import { LegalPage } from '@/components/legal-page'

export default function AccessibilityPage() {
  return (
    <LegalPage
      pageKey="accessibility"
      fallbackTitle="Declaracao de Acessibilidade"
      fallbackMessage="A declaracao de acessibilidade sera publicada em breve."
    />
  )
}
