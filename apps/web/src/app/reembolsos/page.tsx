'use client'

import { LegalPage } from '@/components/legal-page'

export default function RefundPolicyPage() {
  return (
    <LegalPage
      pageKey="refund_policy"
      fallbackTitle="Politica de Pagamentos e Reembolsos"
      fallbackMessage="A politica de pagamentos e reembolsos sera publicada em breve."
    />
  )
}
